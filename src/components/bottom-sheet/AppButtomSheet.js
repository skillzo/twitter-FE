import { View, Text } from "react-native";
import React from "react";

export default function AppButtomSheet() {
  const bottomSheetModalRef = useRef(null);
  return (
    <View>
      <Text>AppButtomSheet</Text>
    </View>
  );
}
